import type { ImageSourcePropType } from 'react-native';

/**
 * Local image bundled by Metro; must live next to the file for reliable resolution.
 */
export const IMAGE_PLACEHOLDER: ImageSourcePropType = require('./logo_default_beta.png');
